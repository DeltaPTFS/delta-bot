discord.py==2.4.0
python-dotenv==1.0.1
# Python 3.12 still includes audioop. Install its replacement only on Python
# versions where the standard-library module was removed.
audioop-lts==0.2.1; python_version >= "3.13"
